# Issue #7 - Feature: configuration file and CLI arguments

## 1. Context & Goal
* **Issue:** #7
* **Objective:** Implement a configuration system for thresholds, polling intervals, and window behavior, supporting CLI overrides and specific persistence rules for hand-made window changes.
* **Status:** Approved (claude-opus-4-6, 2026-08-14)
* **Related Issues:** None

### Open Questions
None.

## 2. Proposed Changes

### 2.1 Files Changed

| File | Change Type | Description |
|------|-------------|-------------|
| `src/boostgauge/config.py` | Add | Configuration data structures, validation, and read/write logic |
| `tests/unit/test_config.py` | Add | Unit tests verifying configuration persistence and read semantics |

### 2.1.1 Path Validation (Mechanical - Auto-Checked)

*Mechanical validation automatically checks:*
- All "Modify" files must exist in repository
- All "Delete" files must exist in repository
- All "Add" files must have existing parent directories
- No placeholder prefixes (`src/`, `lib/`, `app/`) unless directory exists

### 2.2 Dependencies

```toml

# pyproject.toml additions (if any)

# No new dependencies needed for configuration logic
```

### 2.3 Data Structures

```python
from typing import TypedDict, Optional

class PositionConfig(TypedDict):
    x: int
    y: int

class ThresholdConfig(TypedDict):
    yellow: int
    red: int

class Thresholds(TypedDict):
    conpty: ThresholdConfig
    memory_percent: ThresholdConfig
    process_count: ThresholdConfig
    handle_count: ThresholdConfig

class TelltaleWindows(TypedDict):
    short: int
    medium: int
    long: int

class AppConfig(TypedDict):
    polling_interval_seconds: int
    theme: str
    size: int
    opacity: float
    always_on_top: bool
    position: PositionConfig
    thresholds: Thresholds
    telltale_windows: TelltaleWindows
    show_driver_label: bool
    show_digital_readout: bool
    show_session_count: bool

class SessionState(TypedDict):
    config_file_path: str
    active_config: AppConfig
    hand_changed_position: Optional[PositionConfig]
    hand_changed_size: Optional[int]
    reset_config_flag: bool
```

### 2.4 Function Signatures

```python
import logging

def load_config(path: str, reset_flag: bool, cli_overrides: dict) -> AppConfig:
    """Loads config, handles reset and auto-creation, applies CLI overrides. Logs INFO on read/write."""
    ...

def apply_threshold_updates(path: str, current_config: AppConfig) -> AppConfig:
    """Re-reads config from disk and applies ONLY threshold updates to current_config. Logs INFO on reload."""
    ...

def save_session_changes(path: str, hand_changed_position: Optional[PositionConfig], hand_changed_size: Optional[int]) -> None:
    """Writes exactly the hand-changed keys (position/size) to the config file on exit. Logs INFO on save."""
    ...

def mitigate_invalid_config(path: str, raw_data: str) -> None:
    """Handles and isolates config load failures safely, ensuring fallback to defaults without data loss. Logs ERROR on invalid config."""
    ...
```

### 2.5 Logic Flow (Pseudocode)

```
1. On app start, determine config path (Log DEBUG)
2. If --reset-config is set, write default config to path (Log INFO)
3. Read config from path (if missing, write defaults and read) (Log INFO)
4. Apply CLI overrides to the read config to form active session config (Log DEBUG)
5. During session, if thresholds are directly edited, reload only thresholds (Log INFO)
6. On exit, if window position or size were hand-changed, patch ONLY those keys in the config file (Log INFO)
```

### 2.6 Technical Approach

* **Module:** `src/boostgauge/config.py`
* **Pattern:** Immutable session config with patched persistence
* **Key Decisions:** We separate the disk state from the session state to ensure CLI overrides are never persisted and mid-session edits to the config file survive unless explicitly overwritten by a hand-made change.

### 2.7 Architecture Decisions

| Decision | Options Considered | Choice | Rationale |
|----------|-------------------|--------|-----------|
| Config Reloading | Whole file reload, Threshold-only reload | Threshold-only reload | Follows operator ruling #291 that non-threshold keys must defer to the next launch. |
| Persistence Patching | Overwrite whole file, Patch specific keys | Patch specific keys | Operator ruling #249 and #290 require direct file edits to survive unless the user hand-changed that specific key during the session. |

**Architectural Constraints:**
- Must not use `tkinter.Tk()` for rendering tests per `docs/design/0001-test-strategy.md`.

## 3. Requirements

1. First run with no config file creates one with defaults.
2. Launch order is reset, then read, then CLI overrides in memory; CLI value overrides govern the session and the app never writes them to the file.
3. With no CLI overrides, the window opens at the file's position and size.
4. Launched with `--reset-config` and no `--size`, the window opens at the default position and default size; launched with `--reset-config --size N`, it opens at the default position and size N while the file holds the default size.
5. Threshold values (the keys under the `thresholds` object) edited directly in the config file take effect without restart, and reading them never modifies the file.
6. The exit write writes exactly the keys the user hand-changed during the session and touches no other key: a direct file edit made while the app ran survives the exit write for every key the user did not also hand-change.
7. With a direct file edit during the session, the edited key holds the edited value after quit, unless the user also hand-changed that same key, in which case the hand-made value wins.
8. A session with no hand-made changes performs no exit write; if the session also found an existing config file at launch, was launched without `--reset-config`, and the file was not edited directly, the file is byte-identical after quit.
9. Position, no reset, not moved, no direct edits: `position` unchanged.
10. Position, no reset, moved, no direct edits: `position` holds the new position.
11. Position, reset, not moved, no direct edits: `position` holds the default.
12. Position, reset, moved, no direct edits: `position` holds the new position.
13. Size, no reset, no `--size`, not resized, no direct edits: `size` unchanged.
14. Size, no reset, no `--size`, resized, no direct edits: `size` holds the new size.
15. Size, no reset, `--size` given, not resized, no direct edits: `size` unchanged; the CLI value is not written.
16. Size, no reset, `--size` given, resized, no direct edits: `size` holds the new size.
17. Size, reset, no `--size`, not resized, no direct edits: `size` holds the default.
18. Size, reset, no `--size`, resized, no direct edits: `size` holds the new size.
19. Size, reset, `--size` given, not resized, no direct edits: `size` holds the default; the CLI value is not written.
20. Size, reset, `--size` given, resized, no direct edits: `size` holds the new size.
21. Invalid config values produce clear error messages.
22. A non-threshold key (e.g. `theme` or `telltale_windows.short`) edited directly in the config file while the app runs leaves the running session unchanged; the running session never applies it, and the next launch reads whatever the file holds at that point.

## 4. Alternatives Considered

| Option | Pros | Cons | Decision |
|--------|------|------|----------|
| In-memory config dict matching file byte-for-byte | Simple to serialize | Fails the requirement that CLI args don't overwrite file state on save | **Rejected** |
| Separate CLI state and Disk state | Guarantees CLI overrides never leak to disk | Slightly more complex merge logic | **Selected** |

**Rationale:** The selected option enforces the required isolation between the session's active configuration (which includes CLI overrides) and the file's persisted configuration (which must survive untouched except for hand-made changes).

## 5. Data & Fixtures

### 5.1 Data Sources

| Attribute | Value |
|-----------|-------|
| Source | Config JSON File (`config.json`) |
| Format | JSON |
| Size | < 2 KB |
| Refresh | On launch, on explicit threshold reload |
| Copyright/License | N/A |

### 5.2 Data Pipeline

```
Config File ──load_config──► Session State ──apply_threshold_updates──► Active Config
```

### 5.3 Test Fixtures

| Fixture | Source | Notes |
|---------|--------|-------|
| `default_config.json` | Generated | Contains baseline defaults |
| `invalid_config.json` | Hardcoded | Contains syntax errors to trigger validation |

### 5.4 Deployment Pipeline

No external data sources; local file only.

## 6. Diagram
N/A

### 6.1 Mermaid Quality Gate
N/A

### 6.2 Diagram
N/A

## 7. Security & Safety Considerations

### 7.1 Security

| Concern | Mitigation | Status |
|---------|------------|--------|
| Path traversal in config file argument | Validate `--config` path to ensure it's a valid local file path | Addressed |

### 7.2 Safety

| Concern | Mitigation | Status |
|---------|------------|--------|
| Corrupt config file crash | The `mitigate_invalid_config` function validates JSON syntax before applying, rejecting corrupt files | Addressed |

**Fail Mode:** Fail Closed - If the config file is unparseable and cannot be mitigated, the app reports an error and refuses to start with corrupt settings.

**Recovery Strategy:** The user can use `--reset-config` to restore the file to known-good defaults.

## 8. Performance & Cost Considerations

### 8.1 Performance

| Metric | Budget | Approach |
|--------|--------|----------|
| JSON Parse | < 5ms | Use built-in `json` module, file is very small |
| Disk Write | < 10ms | Atomic write using temporary file and rename to avoid partial writes |

**Bottlenecks:** None for simple JSON parsing.

### 8.2 Cost Analysis

| Resource | Unit Cost | Estimated Usage | Monthly Cost |
|----------|-----------|-----------------|--------------|
| N/A | N/A | N/A | $0 |

**Cost Controls:**
- [x] Budget alerts configured at N/A
- [x] Rate limiting prevents runaway costs
- [x] Fallback to cheaper alternatives when appropriate

**Worst-Case Scenario:** N/A

## 9. Legal & Compliance

| Concern | Applies? | Mitigation |
|---------|----------|------------|
| PII/Personal Data | No | N/A |
| Third-Party Licenses | No | N/A |
| Terms of Service | No | N/A |
| Data Retention | No | N/A |
| Export Controls | No | N/A |

**Data Classification:** Public

**Compliance Checklist:**
- [x] No PII stored without consent
- [x] All third-party licenses compatible with project license
- [x] External API usage compliant with provider ToS
- [x] Data retention policy documented

## 10. Verification & Testing

*Ref: [0005-testing-strategy-and-protocols.md](0005-testing-strategy-and-protocols.md) and `docs/design/0001-test-strategy.md`*

**Testing Philosophy:** Strive for 100% automated test coverage. Per `docs/design/0001-test-strategy.md`, tests never instantiate `tkinter.Tk()`. Pass criteria are exact literal values, never placeholders.

### 10.0 Test Plan (TDD - Complete Before Implementation)

**TDD Requirement:** Tests MUST be written and failing BEFORE implementation begins.

| Test ID | Test Description | Expected Behavior | Status |
|---------|------------------|-------------------|--------|
| T010 | First run defaults | Creates file with size `300`, position x=`100` y=`100` | RED |
| T020 | Launch order overrides | CLI overrides apply in session, not written to disk | RED |
| T030 | No overrides load | Window opens at file's exact position and size | RED |
| T040 | Reset config resets to default | `--reset-config` restores size `300`, overrides apply | RED |
| T050 | Threshold hot reload | `apply_threshold_updates` patches thresholds only | RED |
| T060 | Exit writes hand-changed | `save_session_changes` patches only position/size | RED |
| T070 | Direct edit hand-changed collision | Hand-changed position wins over direct file edit | RED |
| T080 | Untouched session byte identical | File byte stream `b'...'` equals pre-launch stream | RED |
| T090 | Position: No reset, not moved | Position `{"x": 100, "y": 100}` remains unchanged | RED |
| T100 | Position: No reset, moved | File position updated to `{"x": 150, "y": 150}` | RED |
| T110 | Position: Reset, not moved | File position restored to default `{"x": 100, "y": 100}` | RED |
| T120 | Position: Reset, moved | File position updated to new `{"x": 150, "y": 150}` | RED |
| T130 | Size: No reset, no CLI, not resized | Size `300` remains unchanged in file | RED |
| T140 | Size: No reset, no CLI, resized | Size updated to `400` in file | RED |
| T150 | Size: No reset, CLI given, not resized| CLI `500` active, file remains `300` | RED |
| T160 | Size: No reset, CLI given, resized | File updated to `400` | RED |
| T170 | Size: Reset, no CLI, not resized | File size restored to `300` | RED |
| T180 | Size: Reset, no CLI, resized | File size updated to `400` | RED |
| T190 | Size: Reset, CLI given, not resized | File size restored to `300`, session uses `500` | RED |
| T200 | Size: Reset, CLI given, resized | File size updated to `400` | RED |
| T210 | Invalid config | Raises `ValueError("Invalid JSON")` | RED |
| T220 | Non-threshold edit ignored | `theme` edit to `light` not applied to session config | RED |

**Coverage Target:** ≥95% for all new code

**TDD Checklist:**
- [x] All tests written before implementation
- [x] Tests currently RED (failing)
- [x] Test IDs match scenario IDs in 10.1
- [x] Test file created at: `tests/unit/test_config.py`

### 10.1 Test Scenarios

| ID | Scenario | Type | Input | Expected Output | Pass Criteria |
|----|----------|------|-------|-----------------|---------------|
| 010 | First run auto-create (REQ-1) | Auto | Empty config dir | File created with defaults | File exists, `"size": 300` |
| 020 | Launch order overrides (REQ-2) | Auto | File `{"size": 200}`, CLI `--size 400` | Active config size 400 | `active_config["size"] == 400`, file `"size": 200` |
| 030 | File coordinates used (REQ-3) | Auto | File `{"size": 200}`, No CLI | Active config size 200 | `active_config["size"] == 200` |
| 040 | Reset behavior (REQ-4) | Auto | `--reset-config --size 500` | File defaults, active 500 | `active_config["size"] == 500`, file `"size": 300` |
| 050 | Threshold reload (REQ-5) | Auto | Edit `thresholds.conpty.red` to `70` | Active config red is 70 | `active_config["thresholds"]["conpty"]["red"] == 70` |
| 060 | Exit writes changed (REQ-6) | Auto | Hand-change size to 400, edit theme to `light` | File size 400, theme `light` | file `"size": 400`, file `"theme": "light"` |
| 070 | Direct edit collision (REQ-7) | Auto | Hand-change size 400, edit size 500 | File size 400 | file `"size": 400` |
| 080 | Untouched session (REQ-8) | Auto | No changes, original content `{"size": 200}` | File remains exactly same | `hash(file_bytes) == expected_hash` |
| 090 | Position not moved (REQ-9) | Auto | No reset, not moved | Position `{"x": 100, "y": 100}` | file `"position": {"x": 100, "y": 100}` |
| 100 | Position moved (REQ-10) | Auto | No reset, hand-moved to 150,150 | Position `{"x": 150, "y": 150}` | file `"position": {"x": 150, "y": 150}` |
| 110 | Position reset not moved (REQ-11) | Auto | Reset, not moved | Position `{"x": 100, "y": 100}` | file `"position": {"x": 100, "y": 100}` |
| 120 | Position reset moved (REQ-12) | Auto | Reset, hand-moved to 150,150 | Position `{"x": 150, "y": 150}` | file `"position": {"x": 150, "y": 150}` |
| 130 | Size not resized (REQ-13) | Auto | No reset, no CLI, not resized | Size `300` | file `"size": 300` |
| 140 | Size resized (REQ-14) | Auto | No reset, no CLI, hand-resized 400 | Size `400` | file `"size": 400` |
| 150 | Size CLI not resized (REQ-15) | Auto | No reset, CLI `--size 500`, not resized | Size `300` | file `"size": 300` |
| 160 | Size CLI resized (REQ-16) | Auto | No reset, CLI `--size 500`, hand-resized 400 | Size `400` | file `"size": 400` |
| 170 | Size reset not resized (REQ-17) | Auto | Reset, no CLI, not resized | Size `300` | file `"size": 300` |
| 180 | Size reset resized (REQ-18) | Auto | Reset, no CLI, hand-resized 400 | Size `400` | file `"size": 400` |
| 190 | Size reset CLI not resized (REQ-19) | Auto | Reset, CLI `--size 500`, not resized | Size `300` | file `"size": 300` |
| 200 | Size reset CLI resized (REQ-20) | Auto | Reset, CLI `--size 500`, hand-resized 400 | Size `400` | file `"size": 400` |
| 210 | Invalid values (REQ-21) | Auto | `"polling_interval_seconds": "fast"` | Error raised | `Raises ValueError("Invalid JSON")` |
| 220 | Non-threshold edit (REQ-22) | Auto | Edit `theme` to `light` while running | Active config theme `dark` | `active_config["theme"] == "dark"` |

### 10.2 Test Commands

```bash

# Run all automated tests
poetry run pytest tests/unit/test_config.py -v

# Run only fast/mocked tests (exclude live)
poetry run pytest tests/unit/test_config.py -v -m "not live"

# Run live integration tests
poetry run pytest tests/unit/test_config.py -v -m live
```

### 10.3 Manual Tests (Only If Unavoidable)

N/A - All scenarios automated.

## 11. Risks & Mitigations

| Risk | Impact | Likelihood | Mitigation |
|------|--------|------------|------------|
| Data corruption during exit write | High | Low | The `save_session_changes` uses atomic temp-file replace; `mitigate_invalid_config` blocks invalid load |
| Race condition if user edits file exactly during exit write | Med | Low | Exit write is a microsecond read-then-patch operation, minimizing the race window |

## 12. Definition of Done

### Code
- [ ] Implementation complete and linted
- [ ] Code comments reference this LLD

### Tests
- [ ] All test scenarios pass
- [ ] Test coverage meets threshold

### Documentation
- [ ] LLD updated with any deviations
- [ ] Implementation Report (0103) completed
- [ ] Test Report (0113) completed if applicable

### Review
- [ ] Code review completed
- [ ] User approval before closing issue

### 12.1 Traceability (Mechanical - Auto-Checked)

*Issue #277: Cross-references are verified programmatically.*

Mechanical validation automatically checks:
- Every file mentioned in this section must appear in Section 2.1
- Every risk mitigation in Section 11 should have a corresponding function in Section 2.4 (warning if not)

---

## Appendix: Review Log

### Orchestrator Review #1 (PENDING)

**Reviewer:** Orchestrator
**Verdict:** PENDING

#### Comments

| ID | Comment | Implemented? |
|----|---------|--------------|
| O1.1 | "Pending initial review" | PENDING |

### Review Summary

| Review | Date | Verdict | Key Issue |
|--------|------|---------|-----------|
| 2 | 2026-08-14 | APPROVED | `claude-opus-4-6` |
| Orchestrator #1 | (auto) | PENDING | Initial draft submission |

**Final Status:** APPROVED