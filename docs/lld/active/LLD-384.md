# 384 - Feature: Bezel Ring Compressed Horizon Returns to Bright

## 1. Context & Goal
* **Issue:** #384
* **Objective:** Ensure the bezel ring's compressed horizon drops to a dark band and returns to bright rather than remaining a continuous dark ramp.
* **Status:** Approved (claude-opus-4-6, 2026-08-30)
* **Related Issues:** #379, #332, #378

### Open Questions
None.

## 2. Proposed Changes

### 2.1 Files Changed

| File | Change Type | Description |
|------|-------------|-------------|
| `src/boostgauge/skins/stingray.py` | Modify | Update the static face rendering logic to reflect a bright return past the dark horizon on the bezel ring. |
| `tests/visual/test_stingray_static.py` | Modify | Add verification for the horizon bright return and ensure #379 regression assertions pass. |

### 2.1.1 Path Validation (Mechanical - Auto-Checked)

Mechanical validation automatically checks:
- All "Modify" files must exist in repository
- All "Delete" files must exist in repository
- All "Add" files must have existing parent directories
- No placeholder prefixes (`src/`, `lib/`, `app/`) unless directory exists

### 2.2 Dependencies

```toml

# pyproject.toml additions (if any)
```

### 2.3 Data Structures

```python

# Pseudocode - NOT implementation
```

### 2.4 Function Signatures

```python

# Signatures only - implementation in source files
def render_face(size: int, values: dict, ss: int = 3) -> Image.Image:
    """Renders the static face and includes the updated bezel ring gradient."""
    ...

def test_bezel_horizon_bright_return() -> None:
    """Asserts the bezel ring horizon profile correctly returns to bright."""
    ...
```

### 2.5 Logic Flow (Pseudocode)

```
1. Render the static face layer
2. For the bezel ring element:
   - Calculate the radial gradient
   - Apply the dark band crossing profile
   - Apply the bright return bracket past the horizon
3. Composite the bezel ring onto the face
```

### 2.6 Technical Approach

* **Module:** `src/boostgauge/skins/stingray.py`
* **Pattern:** Procedural PIL Image generation
* **Key Decisions:** The gradient definition for the bezel ring will be mathematically adjusted to guarantee the sweep re-enters the bright half after dipping to dark, avoiding a continuous ramp to the edge.

### 2.7 Architecture Decisions

| Decision | Options Considered | Choice | Rationale |
|----------|-------------------|--------|-----------|
| Bezel gradient definition | Piecewise linear gradient, radial composite | Radial composite | Consistent with existing stingray skin architecture and allows precise control of the dark band. |

**Architectural Constraints:**
- Must maintain all #379 visual constraints for the bezel ring sweep.

## 3. Requirements

<!-- BEGIN MACHINE-OWNED: source decision table (#2607) -->

### 3.1 Source Decision Table (injected verbatim)

The rows below are carried **verbatim** from the source issue by the derivation itself (#2607). They are machine-owned: the drafter does not write them, and a revision cannot change them. Cite these IDs from the requirements and test-plan sections; do not restate their values.

| ID | Element | Binding value (quoted from the render contract) | Assertion method |
|---|---|---|---|
| S10r | Bezel ring — the horizon returns to bright | "the rolled inner bezel sweeps its surface normal through the strip, so the horizon reappears compressed around the bore" — the sweep re-enters the strip's bright half past the horizon, so the ring's dark crossing is bracketed by bright: within 0.02 R beyond r_d — the first sample with channel mean < 100 — the reflection is back above channel mean 240 (ruled 2026-08-30). Measured anchors, 0.01 R grid: approved dip 90 at 1.11 R, 255 at 1.12 R; the shipped ramp climbs 20–25 channel-mean per 0.01 R and tops at 190, so it cannot satisfy the window | at math angles 90° and 180°, sampling every 0.005 R within [1.05 R, 1.24 R]: let r_d be the smallest sampled radius with channel mean < 100; assert r_d exists, r_d ≤ 1.18 R, and ≥1 sample in (r_d, r_d + 0.02 R] has channel mean > 240 (approved, 0.01 R grid: r_d = 1.11 R with 255 at 1.12 R; the shipped render: r_d = 1.15 R and no sample through 1.24 R exceeds 190) |
| S10g | Bezel ring — the #379 bindings survive the change | the #379 rows S10(a)/(b) remain binding unchanged: dark < 100 and bright > 200 exist per radial at 45°, 90°, 135°, 180°, 225°, 315°; max adjacent step ≥ 150 at 90° and 180°. Evaluated on their own shipped grid — the existing tests' 2 px walk at the test's render size, never S10r's 0.005 R grid (ruled 2026-08-30). S10r and this row are jointly satisfiable, measured on the approved render itself: its max adjacent step on the 2 px walk at 1024 is 238.7 at both 90° and 180° while its dip anchors S10r — the render's transitions are sharp-edged, not continuous ramps (ruled 2026-08-30, refuting the joint-impossibility conflict) | the shipped tests in `tests/visual/test_stingray_static.py` covering the #379 ring assertions run unmodified and pass against the new render — the regression guard is the existing suite itself, on its own grid |

<!-- END MACHINE-OWNED -->

<!-- BEGIN MACHINE-OWNED: source decision table (#384) -->
<!-- END MACHINE-OWNED -->

1. The static face render MUST satisfy the S10r assertion for the bezel ring horizon returning to bright, passing the math angle and sampling constraints specified in S10r.
2. The static face render MUST maintain compliance with existing #379 bindings, as asserted in S10g, ensuring previous visual properties survive the change.

## 4. Alternatives Considered

| Option | Pros | Cons | Decision |
|--------|------|------|----------|
| Modifying existing tests only | None | Does not fix the rendering defect | **Rejected** |
| Adjusting the bezel ring gradient | Fixes the bright return defect | Requires careful tuning | **Selected** |

**Rationale:** The issue explicitly identifies a rendering defect (the horizon never returns to bright). The gradient math must be updated.

## 5. Data & Fixtures

### 5.1 Data Sources

| Attribute | Value |
|-----------|-------|
| Source | N/A |
| Format | N/A |
| Size | N/A |
| Refresh | N/A |
| Copyright/License | N/A |

### 5.2 Data Pipeline

```
N/A
```

### 5.3 Test Fixtures

| Fixture | Source | Notes |
|---------|--------|-------|
| None | N/A | Generative visual tests |

### 5.4 Deployment Pipeline

N/A

## 6. Diagram

N/A

## 7. Security & Safety Considerations

### 7.1 Security

| Concern | Mitigation | Status |
|---------|------------|--------|
| None | N/A | Addressed |

### 7.2 Safety

| Concern | Mitigation | Status |
|---------|------------|--------|
| None | N/A | Addressed |

**Fail Mode:** Fail Closed - N/A

**Recovery Strategy:** N/A

## 8. Performance & Cost Considerations

### 8.1 Performance

| Metric | Budget | Approach |
|--------|--------|----------|
| Latency | N/A | Static rendering |
| Memory | N/A | Fixed size PIL images |
| API Calls | N/A | Local |

**Bottlenecks:** None

### 8.2 Cost Analysis

| Resource | Unit Cost | Estimated Usage | Monthly Cost |
|----------|-----------|-----------------|--------------|
| None | $0 | 0 | $0 |

**Cost Controls:**
- [ ] Budget alerts configured at $0 threshold
- [ ] Rate limiting prevents runaway costs
- [ ] Fallback to cheaper alternatives when appropriate

**Worst-Case Scenario:** N/A

## 9. Legal & Compliance

| Concern | Applies? | Mitigation |
|---------|----------|------------|
| PII/Personal Data | No | N/A |
| Third-Party Licenses | No | N/A |
| Terms of Service | No | N/A |
| Data Retention | No | N/A |
| Export Controls | No | N/A |

**Data Classification:** Public

**Compliance Checklist:**
- [x] No PII stored without consent
- [x] All third-party licenses compatible with project license
- [x] External API usage compliant with provider ToS
- [x] Data retention policy documented

## 10. Verification & Testing

**Testing Philosophy:** Strive for 100% automated test coverage. Manual tests are a last resort for scenarios that genuinely cannot be automated (e.g., visual inspection, hardware interaction). Every scenario marked "Manual" requires justification.

### 10.0 Test Plan (TDD - Complete Before Implementation)

**TDD Requirement:** Tests MUST be written and failing BEFORE implementation begins.

| Test ID | Test Description | Expected Behavior | Status |
|---------|------------------|-------------------|--------|
| T010 | Verify bezel horizon bright return | Asserts S10r conditions | RED |
| T020 | Verify legacy bindings survival | Asserts S10g conditions | RED |

**Coverage Target:** 100% of modified lines in `src/boostgauge/skins/stingray.py`

**TDD Checklist:**
- [ ] All tests written before implementation
- [ ] Tests currently RED (failing)
- [ ] Test IDs match scenario IDs in 10.1
- [ ] Test file created at: `tests/visual/test_stingray_static.py`

### 10.1 Test Scenarios

| ID | Scenario | Type | Input | Expected Output | Pass Criteria |
|----|----------|------|-------|-----------------|---------------|
| 010 | S10r assertion for horizon returning to bright (REQ-1) | Auto | Static face rendered at size 1024 | The dip is bracketed by a bright return | Measurements satisfy S10r |
| 020 | S10g regression guards run and pass (REQ-2) | Auto | Static face rendered at size 1024 | Transitions remain sharp and anchors exist | Measurements satisfy S10g |

### 10.2 Test Commands

```bash

# Run all automated tests
poetry run pytest tests/visual/test_stingray_static.py -v
```

### 10.3 Manual Tests (Only If Unavoidable)

N/A - All scenarios automated.

## 11. Risks & Mitigations

| Risk | Impact | Likelihood | Mitigation |
|------|--------|------------|------------|
| Rendering modifications break #379 assertions | High | Medium | Addressed in `test_bezel_horizon_bright_return` and test suite covering S10g rules. |

## 12. Definition of Done

### Code
- [ ] Implementation complete and linted
- [ ] Code comments reference this LLD

### Tests
- [ ] All test scenarios pass
- [ ] Test coverage meets threshold

### Documentation
- [ ] LLD updated with any deviations
- [ ] Implementation Report (0103) completed
- [ ] Test Report (0113) completed if applicable

### Review
- [ ] Code review completed
- [ ] User approval before closing issue

### 12.1 Traceability (Mechanical - Auto-Checked)

Mechanical validation automatically checks:
- Every file mentioned in this section must appear in Section 2.1
- Every risk mitigation in Section 11 should have a corresponding function in Section 2.4 (warning if not)

### Review Summary

| Review | Date | Verdict | Model |
|--------|------|---------|-------|
| 1 | 2026-08-30 | APPROVED | `claude-opus-4-6` |


**Final Status:** APPROVED
