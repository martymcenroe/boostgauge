# Issue #384 - Feature: the ring's compressed horizon returns to bright

## 1. Context & Goal
* **Issue:** #384
* **Objective:** Fix the visual rendering of the static face's bezel ring so its dark crossing is a band bracketed by bright, rather than an edge into a ramp.
* **Status:** Approved (claude-opus-4-6, 2026-08-30)
* **Related Issues:** #379, #332, #383, #378

### Open Questions
None.

## 2. Proposed Changes

### 2.1 Files Changed

| File | Change Type | Description |
|------|-------------|-------------|
| `src/boostgauge/skins/stingray.py` | Modify | Adjust the static face rendering logic for the bezel ring so the horizon reappears bright past the dark crossing. |
| `tests/visual/test_stingray_static.py` | Modify | Add assertions for the bezel ring's horizon profile and regression guards. |

### 2.1.1 Path Validation (Mechanical - Auto-Checked)

All paths validated.

### 2.2 Dependencies

```toml

# No new dependencies
```

### 2.3 Data Structures

```python

# No new data structures
```

### 2.4 Function Signatures

```python
def render_face(size: int, values: dict, ss: int = 3) -> Image.Image:
    """Render the Stingray static face."""
    ...
```

### 2.5 Logic Flow (Pseudocode)

```
1. In stingray.py during the bezel rendering pass:
2. Compute the reflection mapping or surface normals for the rolled inner bezel.
3. Ensure the sweep re-enters the bright half past the horizon.
4. Composite the bezel layer into the final static face image.
5. Return the rendered PIL.Image.
```

### 2.6 Technical Approach

* **Module:** `src/boostgauge/skins/stingray.py`
* **Key Decisions:** Modifying the static face renderer to correct the bezel ring's gradient/reflection mapping so it strictly matches the approved render's return to brightness, without introducing azimuth dependencies.

### 2.7 Architecture Decisions

| Decision | Options Considered | Choice | Rationale |
|----------|-------------------|--------|-----------|
| Bezel Fix Application | Modify gradient stops in renderer, Post-process image | Modify gradient stops/normals in `stingray.py` | The static renderer must directly produce the correct pixel values conforming to the numeric render contract. |

**Architectural Constraints:**
- The sweep must remain stylized and azimuth-independent.
- Physical mirror consistency per azimuth is not required.
- No `tkinter.Tk()` instantiation allowed in tests.
- Visual assertions must assert against literal values, not baselines.

## 3. Requirements

<!-- BEGIN MACHINE-OWNED: source decision table (#2607) -->

### 3.1 Source Decision Table (injected verbatim)

The rows below are carried **verbatim** from the source issue by the derivation itself (#2607). They are machine-owned: the drafter does not write them, and a revision cannot change them. Cite these IDs from the requirements and test-plan sections; do not restate their values.

| ID | Element | Binding value (quoted from the render contract) | Assertion method |
|---|---|---|---|
| S10r | Bezel ring — the horizon returns to bright | "the rolled inner bezel sweeps its surface normal through the strip, so the horizon reappears compressed around the bore" — the sweep re-enters the strip's bright half past the horizon, so the ring's dark crossing is bracketed by bright: within 0.02 R beyond r_d — the first sample with channel mean < 100 — the reflection is back above channel mean 240 (ruled 2026-08-30). Measured anchors, 0.01 R grid: approved dip 90 at 1.11 R, 255 at 1.12 R; the shipped ramp climbs 20–25 channel-mean per 0.01 R and tops at 190, so it cannot satisfy the window | at math angles 90° and 180°, sampling every 0.005 R within [1.05 R, 1.24 R]: let r_d be the smallest sampled radius with channel mean < 100; assert r_d exists, r_d ≤ 1.18 R, and ≥1 sample in (r_d, r_d + 0.02 R] has channel mean > 240 (approved, 0.01 R grid: r_d = 1.11 R with 255 at 1.12 R; the shipped render: r_d = 1.15 R and no sample through 1.24 R exceeds 190) |
| S10g | Bezel ring — the #379 bindings survive the change | the #379 S10(a)/(b) bindings hold on the new render: on each sampled radial the ring carries both a dark sample (channel mean < 100) and a bright one (channel mean > 200), and at 90° and 180° the largest step between adjacent samples on a 2 px walk is ≥ 150. Evaluated on the 2 px grid, never S10r's 0.005 R grid (ruled 2026-08-30). Jointly satisfiable with S10r, measured on the approved render itself: its max adjacent 2 px step at 1024 is 238.7 at both 90° and 180° while its dip anchors S10r — the transitions are sharp-edged, not continuous ramps (ruled 2026-08-30, refuting the joint-impossibility conflict) | at math angles 45°, 90°, 135°, 180°, 225°, 315°, sampling every 2 px within [1.035 R, 1.24 R]: assert every listed angle carries ≥1 sample with channel mean < 100 and ≥1 sample with channel mean > 200; and at 90° and 180° assert the maximum absolute difference between adjacent samples is ≥ 150 (approved render: 238.7 at both; the #379 ship also satisfies this row — it is a regression guard, not a discriminator) |

<!-- END MACHINE-OWNED -->

1. The rendered bezel ring's horizon MUST return to bright past the dark crossing, satisfying S10r.
2. The legacy bezel ring assertions MUST survive the change and continue to pass, satisfying S10g.

## 4. Alternatives Considered

| Option | Pros | Cons | Decision |
|--------|------|------|----------|
| Adjust renderer logic in `stingray.py` | Fixes the root cause of the visual defect directly in the rendering pipeline. | None. | **Selected** |

**Rationale:** The rendering logic itself is producing a ramp instead of a band. Correcting the logic directly is the only way to satisfy the contract.

## 5. Data & Fixtures

### 5.1 Data Sources

| Attribute | Value |
|-----------|-------|
| Source | N/A |
| Format | N/A |
| Size | N/A |
| Refresh | N/A |
| Copyright/License | N/A |

### 5.2 Data Pipeline

```
N/A ──N/A──► N/A ──N/A──► N/A
```

### 5.3 Test Fixtures

| Fixture | Source | Notes |
|---------|--------|-------|
| N/A | N/A | Tests will invoke the renderer to generate images in memory. |

### 5.4 Deployment Pipeline

N/A

## 6. Diagram

N/A

### 6.1 Mermaid Quality Gate

N/A

### 6.2 Diagram

N/A

## 7. Security & Safety Considerations

### 7.1 Security

| Concern | Mitigation | Status |
|---------|------------|--------|
| None | N/A | Addressed |

### 7.2 Safety

| Concern | Mitigation | Status |
|---------|------------|--------|
| None | N/A | Addressed |

**Fail Mode:** Fail Closed - N/A

**Recovery Strategy:** N/A

## 8. Performance & Cost Considerations

### 8.1 Performance

| Metric | Budget | Approach |
|--------|--------|----------|
| Rendering Time | No significant increase | Tweaking visual rendering logic parameters has negligible performance impact. |

**Bottlenecks:** None.

### 8.2 Cost Analysis

| Resource | Unit Cost | Estimated Usage | Monthly Cost |
|----------|-----------|-----------------|--------------|
| N/A | N/A | N/A | $0 |

**Cost Controls:**
- [x] Budget alerts configured at $0 threshold
- [x] Rate limiting prevents runaway costs
- [x] Fallback to cheaper alternatives when appropriate

**Worst-Case Scenario:** N/A

## 9. Legal & Compliance

| Concern | Applies? | Mitigation |
|---------|----------|------------|
| PII/Personal Data | No | N/A |
| Third-Party Licenses | No | N/A |
| Terms of Service | No | N/A |
| Data Retention | No | N/A |
| Export Controls | No | N/A |

**Data Classification:** Public

**Compliance Checklist:**
- [x] No PII stored without consent
- [x] All third-party licenses compatible with project license
- [x] External API usage compliant with provider ToS
- [x] Data retention policy documented

## 10. Verification & Testing

**Testing Philosophy:** Strive for 100% automated test coverage. Manual tests are a last resort for scenarios that genuinely cannot be automated (e.g., visual inspection, hardware interaction). Every scenario marked "Manual" requires justification.

### 10.0 Test Plan (TDD - Complete Before Implementation)

| Test ID | Test Description | Expected Behavior | Status |
|---------|------------------|-------------------|--------|
| T010 | Verify horizon returns to bright | Passes the S10r assertion | RED |
| T020 | Verify legacy bindings survive | Passes the S10g assertion | RED |

**Coverage Target:** ≥95% for all new code

**TDD Checklist:**
- [x] All tests written before implementation
- [x] Tests currently RED (failing)
- [x] Test IDs match scenario IDs in 10.1
- [x] Test file created at: `tests/visual/test_stingray_static.py`

### 10.1 Test Scenarios

| ID | Scenario | Type | Input | Expected Output | Pass Criteria |
|----|----------|------|-------|-----------------|---------------|
| 010 | Verify horizon returns to bright (REQ-1) | Auto | Rendered static face | `PIL.Image` | Satisfies S10r |
| 020 | Verify legacy bindings survive (REQ-2) | Auto | Rendered static face | `PIL.Image` | Satisfies S10g |

### 10.2 Test Commands

```bash

# Run all automated tests
poetry run pytest tests/visual/test_stingray_static.py -v

# Run only fast/mocked tests (exclude live)
poetry run pytest tests/visual/test_stingray_static.py -v -m "not live"

# Run live integration tests
poetry run pytest tests/visual/test_stingray_static.py -v -m live
```

### 10.3 Manual Tests (Only If Unavoidable)

N/A - All scenarios automated.

## 11. Risks & Mitigations

| Risk | Impact | Likelihood | Mitigation |
|------|--------|------------|------------|
| Visual changes inadvertently break legacy bindings | Med | Low | The `render_face` adjustments are verified by comprehensive visual assertions. Scenario 020 acts as a strict regression guard. |

## 12. Definition of Done

### Code
- [ ] Implementation complete and linted
- [ ] Code comments reference this LLD

### Tests
- [ ] All test scenarios pass
- [ ] Test coverage meets threshold

### Documentation
- [ ] LLD updated with any deviations
- [ ] Implementation Report (0103) completed
- [ ] Test Report (0113) completed if applicable

### Review
- [ ] Code review completed
- [ ] User approval before closing issue

### 12.1 Traceability (Mechanical - Auto-Checked)

- `src/boostgauge/skins/stingray.py` appears in Section 2.1
- `tests/visual/test_stingray_static.py` appears in Section 2.1
- `render_face` in Section 2.4 corresponds to the mitigation in Section 11

---

## Appendix: Review Log

### Review Summary

| Review | Date | Verdict | Key Issue |
|--------|------|---------|-----------|
| 1 | 2026-08-30 | APPROVED | `claude-opus-4-6` |
| None | (auto) | PENDING | None |

**Final Status:** APPROVED