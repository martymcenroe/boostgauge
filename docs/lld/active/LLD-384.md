# Issue #384 - Feature: the ring's compressed horizon returns to bright

<!-- Template Metadata
Last Updated: 2026-02-02
Updated By: Issue #117 fix
Update Reason: Moved Verification & Testing to Section 10 (was Section 11) to match 0702c review prompt and testing workflow expectations
Previous: Added sections based on 80 blocking issues from 164 governance verdicts (2026-02-01)
-->

## 1. Context & Goal
* **Issue:** #384
* **Objective:** Ensure the bezel ring's dark horizon is bracketed by bright reflections, replacing the current ramp with a sharp dip and return to bright.
* **Status:** Approved (claude-opus-4-6, 2026-08-30)
* **Related Issues:** #379, #332, #378

### Open Questions
None.

## 2. Proposed Changes

### 2.1 Files Changed

| File | Change Type | Description |
|------|-------------|-------------|
| `src/boostgauge/skins/stingray.py` | Modify | Adjust rendering logic to implement the bright return past the horizon. |
| `tests/visual/test_stingray_static.py` | Modify | Add assertion for the horizon return at 0.005 R intervals. |

### 2.1.1 Path Validation (Mechanical - Auto-Checked)

*Issue #277: Before human or Gemini review, paths are verified programmatically.*

Mechanical validation automatically checks:
- All "Modify" files must exist in repository
- All "Delete" files must exist in repository
- All "Add" files must have existing parent directories
- No placeholder prefixes (`src/`, `lib/`, `app/`) unless directory exists

**If validation fails, the LLD is BLOCKED before reaching review.**

### 2.2 Dependencies

```toml

# No new dependencies
```

### 2.3 Data Structures

```python

# No new data structures
```

### 2.4 Function Signatures

```python

# Signatures only - implementation in source files
def render_face(size: int, values: dict, ss: int = 3) -> Image.Image:
    """Renders the static face, implementing the S10r bezel ring horizon return."""
    ...

def test_bezel_ring_horizon_return() -> None:
    """Asserts row S10r conditions on the bezel ring reflection."""
    ...
```

### 2.5 Logic Flow (Pseudocode)

```
1. In src/boostgauge/skins/stingray.py (render_face):
   - Modify the bezel ring gradient math for the inner sweep.
   - Ensure the reflection drops below 100 and returns to > 240 within 0.02 R of the dip.
2. In tests/visual/test_stingray_static.py (test_bezel_ring_horizon_return):
   - Render the face at the test size (e.g., 1024).
   - Sample at math angles 90° and 180°.
   - Sample every 0.005 R within [1.05 R, 1.24 R].
   - Find r_d (the smallest sampled radius with channel mean < 100).
   - Assert r_d exists and r_d <= 1.18 R.
   - Assert >=1 sample in (r_d, r_d + 0.02 R] has channel mean > 240.
```

### 2.6 Technical Approach

* **Module:** `src/boostgauge/skins/stingray.py`
* **Pattern:** N/A
* **Key Decisions:** Implement the aesthetic change mathematically within the radial sweep logic to maintain resolution independence and satisfy S10r without disrupting the bindings in S10g.

### 2.7 Architecture Decisions

| Decision | Options Considered | Choice | Rationale |
|----------|-------------------|--------|-----------|
| Bezel gradient implementation | Mathematical radial sweep, explicit masking | Mathematical radial sweep | Maintains consistency with existing stylized sweeps and satisfies resolution-independent constraints. |

**Architectural Constraints:**
- Must not instantiate `tkinter.Tk()` in tests (Option C from test strategy).
- The existing #379 visual bindings must survive unchanged (S10g).

## 3. Requirements

<!-- BEGIN MACHINE-OWNED: source decision table (#2607) -->

### 3.1 Source Decision Table (injected verbatim)

The rows below are carried **verbatim** from the source issue by the derivation itself (#2607). They are machine-owned: the drafter does not write them, and a revision cannot change them. Cite these IDs from the requirements and test-plan sections; do not restate their values.

| ID | Element | Binding value (quoted from the render contract) | Assertion method |
|---|---|---|---|
| S10r | Bezel ring — the horizon returns to bright | "the rolled inner bezel sweeps its surface normal through the strip, so the horizon reappears compressed around the bore" — the sweep re-enters the strip's bright half past the horizon, so the ring's dark crossing is bracketed by bright: within 0.02 R beyond r_d — the first sample with channel mean < 100 — the reflection is back above channel mean 240 (ruled 2026-08-30). Measured anchors, 0.01 R grid: approved dip 90 at 1.11 R, 255 at 1.12 R; the shipped ramp climbs 20–25 channel-mean per 0.01 R and tops at 190, so it cannot satisfy the window | at math angles 90° and 180°, sampling every 0.005 R within [1.05 R, 1.24 R]: let r_d be the smallest sampled radius with channel mean < 100; assert r_d exists, r_d ≤ 1.18 R, and ≥1 sample in (r_d, r_d + 0.02 R] has channel mean > 240 (approved, 0.01 R grid: r_d = 1.11 R with 255 at 1.12 R; the shipped render: r_d = 1.15 R and no sample through 1.24 R exceeds 190) |
| S10g | Bezel ring — the #379 bindings survive the change | the #379 rows S10(a)/(b) remain binding unchanged: dark < 100 and bright > 200 exist per radial at 45°, 90°, 135°, 180°, 225°, 315°; max adjacent step ≥ 150 at 90° and 180°. Evaluated on their own shipped grid — the existing tests' 2 px walk at the test's render size, never S10r's 0.005 R grid (ruled 2026-08-30). S10r and this row are jointly satisfiable, measured on the approved render itself: its max adjacent step on the 2 px walk at 1024 is 238.7 at both 90° and 180° while its dip anchors S10r — the render's transitions are sharp-edged, not continuous ramps (ruled 2026-08-30, refuting the joint-impossibility conflict) | the shipped tests in `tests/visual/test_stingray_static.py` covering the #379 ring assertions run unmodified and pass against the new render — the regression guard is the existing suite itself, on its own grid |

<!-- END MACHINE-OWNED -->

1. The static face render MUST satisfy the mathematical constraints for the bezel ring's bright return specified in row S10r of the decision table.
2. The existing static face tests MUST pass unmodified against the new render, satisfying row S10g of the decision table.

## 4. Alternatives Considered

| Option | Pros | Cons | Decision |
|--------|------|------|----------|
| Adjust gradient using explicit masks | Direct pixel control | Prone to aliasing, difficult to scale | Rejected |
| Adjust mathematical radial formula | Smooth, resolution independent | Requires tuning formulas | **Selected** |

**Rationale:** The mathematical approach scales cleanly to any render size and aligns with the existing drawing methodology.

## 5. Data & Fixtures

### 5.1 Data Sources

| Attribute | Value |
|-----------|-------|
| Source | N/A |
| Format | N/A |
| Size | N/A |
| Refresh | N/A |
| Copyright/License | N/A |

### 5.2 Data Pipeline

```
N/A
```

### 5.3 Test Fixtures

| Fixture | Source | Notes |
|---------|--------|-------|
| N/A | N/A | Tests rely on images generated dynamically by the renderer. |

### 5.4 Deployment Pipeline

N/A

## 6. Diagram

N/A

### 6.1 Mermaid Quality Gate

**Auto-Inspection Results:**
```
- Touching elements: [ ] None / [ ] Found: N/A
- Hidden lines: [ ] None / [ ] Found: N/A
- Label readability: [ ] Pass / [ ] Issue: N/A
- Flow clarity: [ ] Clear / [ ] Issue: N/A
```

### 6.2 Diagram

N/A

## 7. Security & Safety Considerations

### 7.1 Security

| Concern | Mitigation | Status |
|---------|------------|--------|
| None | N/A | Addressed |

### 7.2 Safety

| Concern | Mitigation | Status |
|---------|------------|--------|
| None | N/A | Addressed |

**Fail Mode:** Fail Closed - N/A

**Recovery Strategy:** N/A

## 8. Performance & Cost Considerations

### 8.1 Performance

| Metric | Budget | Approach |
|--------|--------|----------|
| Latency | < 500ms | Mathematical formula adjustments have negligible impact on render time. |
| Memory | < 128MB | Render at standard sizes, no extra allocations. |
| API Calls | 0 | N/A |

**Bottlenecks:** None identified.

### 8.2 Cost Analysis

| Resource | Unit Cost | Estimated Usage | Monthly Cost |
|----------|-----------|-----------------|--------------|
| N/A | N/A | N/A | $0 |

**Cost Controls:**
- [x] Budget alerts configured at $0 threshold
- [x] Rate limiting prevents runaway costs
- [x] Fallback to cheaper alternatives when appropriate

**Worst-Case Scenario:** N/A

## 9. Legal & Compliance

| Concern | Applies? | Mitigation |
|---------|----------|------------|
| PII/Personal Data | No | N/A |
| Third-Party Licenses | No | N/A |
| Terms of Service | No | N/A |
| Data Retention | No | N/A |
| Export Controls | No | N/A |

**Data Classification:** Public

**Compliance Checklist:**
- [x] No PII stored without consent
- [x] All third-party licenses compatible with project license
- [x] External API usage compliant with provider ToS
- [x] Data retention policy documented

## 10. Verification & Testing

**Testing Philosophy:** Strive for 100% automated test coverage. Manual tests are a last resort for scenarios that genuinely cannot be automated (e.g., visual inspection, hardware interaction). Every scenario marked "Manual" requires justification.

### 10.0 Test Plan (TDD - Complete Before Implementation)

| Test ID | Test Description | Expected Behavior | Status |
|---------|------------------|-------------------|--------|
| T010 | Assert bezel ring horizon returns to bright | The reflection goes bright past the horizon | RED |
| T020 | Verify #379 visual bindings survive | Existing visual tests pass against the new render | RED |

**Coverage Target:** 100% (No `fail_under` found, visually asserting all newly written lines)

**TDD Checklist:**
- [x] All tests written before implementation
- [x] Tests currently RED (failing)
- [x] Test IDs match scenario IDs in 10.1
- [x] Test file created at: `tests/visual/test_stingray_static.py`

### 10.1 Test Scenarios

| ID | Scenario | Type | Input | Expected Output | Pass Criteria |
|----|----------|------|-------|-----------------|---------------|
| 010 | Bezel ring horizon returns to bright (REQ-1) | Auto | Rendered static face | S10r conditions pass | r_d (smallest R with mean < 100) exists, r_d ≤ 1.18 R, and ≥1 sample in (r_d, r_d + 0.02 R] has mean > 240, sampled every 0.005 R in [1.05 R, 1.24 R] at 90° and 180° |
| 020 | Existing #379 assertions survive (REQ-2) | Auto | Rendered static face | Existing #379 assertions pass | dark < 100 and bright > 200 exist per radial at 45°, 90°, 135°, 180°, 225°, 315°; max adjacent step ≥ 150 at 90° and 180° |

### 10.2 Test Commands

```bash

# Run visual tests
poetry run pytest tests/visual/test_stingray_static.py -v
```

### 10.3 Manual Tests (Only If Unavoidable)

N/A - All scenarios automated.

## 11. Risks & Mitigations

| Risk | Impact | Likelihood | Mitigation |
|------|--------|------------|------------|
| Render adjustments break S10g | High | Medium | Execute `test_bezel_ring_horizon_return` to ensure S10r is met, and verify the existing test suite passes untouched to protect S10g. |

## 12. Definition of Done

### Code
- [ ] Implementation complete and linted
- [ ] Code comments reference this LLD

### Tests
- [ ] All test scenarios pass
- [ ] Test coverage meets threshold

### Documentation
- [ ] LLD updated with any deviations
- [ ] Implementation Report (0103) completed
- [ ] Test Report (0113) completed if applicable

### Review
- [ ] Code review completed
- [ ] User approval before closing issue

### 12.1 Traceability (Mechanical - Auto-Checked)

*Issue #277: Cross-references are verified programmatically.*

Mechanical validation automatically checks:
- Every file mentioned in this section must appear in Section 2.1
- Every risk mitigation in Section 11 should have a corresponding function in Section 2.4 (warning if not)

---

## Appendix: Review Log

### Review Summary

| Review | Date | Verdict | Key Issue |
|--------|------|---------|-----------|
| 1 | 2026-08-30 | APPROVED | `claude-opus-4-6` |
| Gemini #1 | (auto) | APPROVED | None |

**Final Status:** APPROVED