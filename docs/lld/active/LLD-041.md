# 41 - Feature: Telltale peak-hold needle logic (pure, no GUI)

## 1. Context & Goal
* **Issue:** #41
* **Objective:** Add the peak-hold "telltale" logic as a pure class that tracks the maximum value reached over a sliding time window with optional linear decay.
* **Status:** Approved (claude-opus-4-6, 2026-09-01)
* **Related Issues:** #1, #2, #7, #35, #36, #125, #315, #424

### Open Questions
None.

## 2. Proposed Changes

### 2.1 Files Changed

| File | Change Type | Description |
|------|-------------|-------------|
| `src/boostgauge/telltale.py` | Add | Core class providing the peak-hold tracking logic and window mathematics. |
| `tests/unit/test_telltale.py` | Add | Unit tier test suite providing 100% coverage via exact value assertions for all acceptance criteria. |

### 2.1.1 Path Validation (Mechanical - Auto-Checked)

Mechanical validation automatically checks:
- All "Modify" files must exist in repository
- All "Delete" files must exist in repository
- All "Add" files must have existing parent directories
- No placeholder prefixes (`src/`, `lib/`, `app/`) unless directory exists

### 2.2 Dependencies

```toml

# pyproject.toml additions (if any)
```
None.

### 2.3 Data Structures

```python

# Pseudocode - NOT implementation
class TelltaleState:
    window: float | None
    decay_rate: float | None
    reference_time: float | None
    # A deque to store (timestamp, value) pairs sorted by timestamp
    in_window_samples: deque[tuple[float, float]]
    # A list to store the tracks of departed highs: (departure_time, departure_value)
    departed_tracks: list[tuple[float, float]]
```

### 2.4 Function Signatures

```python
def _positive_number(x) -> bool:
    """Return True if x is a number strictly greater than zero."""
    ...

class Telltale:
    def __init__(self, window: float | None, decay_rate: float | None = None) -> None:
        """Initialize telltale with window duration and optional decay rate."""
        ...

    def update(self, timestamp: float, value: float) -> None:
        """Feed one sample. Timestamps must not decrease since the last reset."""
        ...

    def reset(self) -> None:
        """Discard history — samples, tracks, and the fed-timestamp record. Configuration survives."""
        ...

    def current_peak(self) -> float | None:
        """The maximum contribution among held samples; None while nothing is held. Pure."""
        ...

    def _age_out(self) -> None:
        """Move samples whose age now exceeds the window out of the deque."""
        ...

    def _track_contribution(self, track: tuple[float, float]) -> float:
        """Calculate the decayed value of a departed track."""
        ...
```

### 2.5 Logic Flow (Pseudocode)

```
1. On update(timestamp, value):
   - Validate timestamp >= reference_time. Raise ValueError if not.
   - Update reference_time to timestamp.
   - Run _age_out() to prune old samples into departed_tracks.
   - Remove any newer samples from deque that have value <= the incoming value (monotonic constraint).
   - Append (timestamp, value) to deque.
2. On current_peak():
   - Return None if reference_time is None.
   - Yield the max over the deque values.
   - Yield the max over the departed_tracks calculated via _track_contribution().
   - Return the highest yielded value.
```

### 2.6 Technical Approach

* **Module:** `src/boostgauge/telltale.py`
* **Pattern:** Monotonic deque
* **Key Decisions:** Operations are kept amortized O(1) by dropping samples from the deque that arrive earlier but have a lower value than a newly updated sample. Time progresses purely via explicit timestamps fed to `update()`, making reads fully deterministic and entirely detached from system clocks.

### 2.7 Architecture Decisions

| Decision | Options Considered | Choice | Rationale |
|----------|-------------------|--------|-----------|
| Time propagation | Wall-clock `time.time()`, Explicit fed timestamps | Explicit fed timestamps | Enables pure determinism and eliminates implicit clock reads; makes every test a direct value assertion without mocking. |
| Dropped sample retention | Retain all samples, Prune via Monotonic Deque | Prune via Monotonic Deque | Older samples with lower values can never be the peak or out-decay a newer higher sample, allowing O(1) retention without memory bloat. |

**Architectural Constraints:**
- No visual GUI, `tkinter`, or output rendering allowed within this tier.
- No `psutil` or config file parsing allowed.
- Must conform to 100% test coverage target.

## 3. Requirements

1. The telltale shall be constructed with a window that is either a number of seconds greater than zero or None meaning all-time, and optionally a decay_rate in units per second greater than zero.
2. WHEN `update(timestamp, value)` is called with a timestamp no lower than every timestamp fed since the most recent `reset()` (or since construction, if never reset), the telltale shall add the sample to its history and set the reference time to the greatest such timestamp.
3. WHEN `reset()` is called, the telltale shall discard its entire history — every held sample, every decay track, and the record of fed timestamps, and nothing else — and shall leave its configuration (`window`, `decay_rate`) unchanged, so that the instance is observationally identical to a freshly constructed one with the same configuration.
4. WHEN `current_peak()` is called, the telltale shall modify no state.
5. WHILE no sample is held — before the first `update()`, and again after any `reset()` until the next `update()` — `current_peak()` shall return None.
6. WHILE at least one sample is held, `current_peak()` shall return the maximum contribution among held samples, per the Peak semantics section.
7. WHERE `window` is None, no sample shall ever age out, so the peak is the maximum value fed since the most recent reset and decay never engages.
8. WHERE `decay_rate` is unset, an aged-out sample shall be dropped from the peak read entirely — excluded from the maximum, not counted as a zero.
9. WHERE `decay_rate` is set, an aged-out sample shall contribute its value minus `decay_rate` times the elapsed time since its departure.
10. IF `update()` is called with a timestamp lower than a timestamp fed since the most recent `reset()` (or since construction, if never reset) THEN the telltale shall raise ValueError and leave its history unchanged.
11. IF constructed with a window that is neither None nor a number greater than zero THEN the constructor shall raise ValueError.
12. IF constructed with a decay_rate that is given (not None) and is not a number greater than zero THEN the constructor shall raise ValueError.
13. WHEN two samples share a timestamp, the telltale shall hold both, and the reference time does not move.

## 4. Alternatives Considered

| Option | Pros | Cons | Decision |
|--------|------|------|----------|
| Retain full array of samples | Trivially implements all math definitions literally | Unbounded memory growth; O(N) linear sweep for peak | **Rejected** |
| Monotonic deque with pruned dominant decay tracks | Bounded memory; O(1) amortized reads | Requires rigorous attention to edge cases on drop logic | **Selected** |

**Rationale:** The O(1) amortized constraint forces dropping dominated samples. A track's slope is uniformly `decay_rate`, preventing intersections, which guarantees ordering is preserved across all time.

## 5. Data & Fixtures

### 5.1 Data Sources

| Attribute | Value |
|-----------|-------|
| Source | Function inputs via `update(timestamp, value)` |
| Format | Native Python `float` primitives |
| Size | Single 64-bit IEEE-754 values per update |
| Refresh | On-demand via explicit call |
| Copyright/License | N/A |

### 5.2 Data Pipeline

```
Method Arguments ──in-memory──► Deque/Track arrays ──in-memory──► Float Output
```

### 5.3 Test Fixtures

| Fixture | Source | Notes |
|---------|--------|-------|
| N/A | Hardcoded values in tests | Pure unit tests use simple numeric literals |

### 5.4 Deployment Pipeline

Pure logic class. Executed directly within the application process.

## 6. Diagram

N/A

### 6.1 Mermaid Quality Gate

N/A

### 6.2 Diagram

N/A

## 7. Security & Safety Considerations

### 7.1 Security

| Concern | Mitigation | Status |
|---------|------------|--------|
| Input injection | N/A - System runs internal primitives only | Addressed |

### 7.2 Safety

| Concern | Mitigation | Status |
|---------|------------|--------|
| Memory exhaustion from history | Monotonic deque bounds held items by pruning old/lower values. Departed tracks also pruned based on dominance. | Addressed |
| Time travel / corrupt history | `update()` raises ValueError on monotonically decreasing timestamps without corrupting internal state. | Addressed |

**Fail Mode:** Fail Closed - Decreasing timestamps raise loud `ValueError` rather than implicitly corrupting the sliding window arithmetic.

**Recovery Strategy:** Calling `reset()` clears the history and enables accepting an entirely fresh timeline.

## 8. Performance & Cost Considerations

### 8.1 Performance

| Metric | Budget | Approach |
|--------|--------|----------|
| Latency | < 1ms per update | Amortized O(1) by pushing and popping only undominated paths in `deque`. |
| Memory | < 1MB | Only retaining strict peak dominators bounds item counts safely. |
| CPU | Minimal | No OS queries; no clock polling. Pure arithmetic. |

**Bottlenecks:** None given O(1) constraints.

### 8.2 Cost Analysis

| Resource | Unit Cost | Estimated Usage | Monthly Cost |
|----------|-----------|-----------------|--------------|
| N/A | N/A | N/A | $0 |

**Cost Controls:**
- [ ] Budget alerts configured at N/A
- [ ] Rate limiting prevents runaway costs
- [ ] Fallback to cheaper alternatives when appropriate

**Worst-Case Scenario:** High frequency updates are safely absorbed by O(1) amortized queue operations.

## 9. Legal & Compliance

| Concern | Applies? | Mitigation |
|---------|----------|------------|
| PII/Personal Data | No | N/A |
| Third-Party Licenses | No | N/A |
| Terms of Service | No | N/A |
| Data Retention | No | N/A |
| Export Controls | No | N/A |

**Data Classification:** Public

**Compliance Checklist:**
- [x] No PII stored without consent
- [x] All third-party licenses compatible with project license
- [x] External API usage compliant with provider ToS
- [x] Data retention policy documented

## 10. Verification & Testing

**Testing Philosophy:** Strive for 100% automated test coverage. Manual tests are a last resort for scenarios that genuinely cannot be automated.

### 10.0 Test Plan (TDD - Complete Before Implementation)

| Test ID | Test Description | Expected Behavior | Status |
|---------|------------------|-------------------|--------|
| T010 | N1_fresh_instance_reads_none | Initial read returns `None` | RED |
| T020 | N2_reset_reads_none | Returns `None` post-reset | RED |
| T030 | A1_single_sample | First sample becomes peak | RED |
| T040 | A2_rising_series_registers_immediately | Peak tracks rising input instantly | RED |
| T050 | A3_in_window_values_never_decay | Value stays undecayed within window | RED |
| T060 | A4_closed_boundary_age_exactly_window_is_in_window | Value exactly at window limit survives | RED |
| T070 | A5_equal_timestamps_hold_both | Accepts overlapping timestamps cleanly | RED |
| T080 | A6_reset_discards_decay_tracks | Tracks are dropped entirely on reset | RED |
| T090 | A7_history_unchanged_after_rejected_update | Failed backward timestamp changes nothing | RED |
| T100 | A8_monotonic_contract_restarts_at_reset | Allows lower timestamps across `reset` barrier | RED |
| T110 | H1_departed_high_is_dropped_instantly | Drops directly to active max with decay unset | RED |
| T120 | H2_exclusion_is_not_a_zero | Negative window peak isn't zero-floored | RED |
| T130 | D1_decay_track | Extrapolates decay accurately past window limit | RED |
| T140 | D2_window_maximum_is_the_only_floor | Won't decay below the active window's max | RED |
| T150 | D3_new_high_beats_the_track | Active window outpaces falling track | RED |
| T160 | D4_every_departed_high_keeps_its_own_track | Evaluates highest among multiple overlapping traces | RED |
| T170 | D5_reads_are_pure | Repeated peak calls give identical results | RED |
| T180 | T1_all_time_never_ages_out | Peak retained indefinitely | RED |
| T190 | T2_all_time_ignores_decay | Decay configuration ignored on infinite window | RED |
| T200 | V1_window_must_be_positive | Non-positive window triggers Exception | RED |
| T210 | V2_decay_rate_must_be_positive | Negative/zero decay triggers Exception | RED |
| T220 | V3_decreasing_timestamp_raises | Backwards timestamp throws Exception | RED |
| T230 | V4_equal_timestamp_is_accepted | Safe ingestion of same-time ticks | RED |
| T240 | Constructor_accepts_valid_args | Creation bounds are properly mapped | RED |

**Coverage Target:** 100% for all new code

**TDD Checklist:**
- [x] All tests written before implementation
- [x] Tests currently RED (failing)
- [x] Test IDs match scenario IDs in 10.1
- [x] Test file created at: `tests/unit/test_telltale.py`

### 10.1 Test Scenarios

| ID | Scenario | Type | Input | Expected Output | Pass Criteria |
|----|----------|------|-------|-----------------|---------------|
| 010 | N1 - Fresh instance reads None (REQ-5) | Auto | `Telltale(10.0)` | `None` | `current_peak()` returns `None` |
| 020 | N2 - Reset reads None (REQ-3) | Auto | `update(0.0, 100.0)`, `reset()` | `None` | `current_peak()` returns `None` |
| 030 | A1 - Single sample registers (REQ-2) | Auto | `Telltale(10.0)`, `update(3.0, 42.5)` | `42.5` | `current_peak()` returns `42.5` |
| 040 | A2 - Rising series registers immediately (REQ-2) | Auto | `(0.0, 10.0), (1.0, 20.0), (2.0, 30.0)` | `30.0` | `current_peak()` returns `30.0` |
| 050 | A3 - In-window values never decay (REQ-6) | Auto | `decay_rate=15.0, (0.0, 100.0), (5.0, 0.0)` | `100.0` | `current_peak()` returns `100.0` |
| 060 | A4 - Closed boundary (REQ-6) | Auto | `(0.0, 100.0), (10.0, 0.0)` | `100.0` | `current_peak()` returns `100.0` |
| 070 | A5 - Equal timestamps hold both (REQ-13) | Auto | `(5.0, 1.0), (5.0, 3.0)` | `3.0` | `current_peak()` returns `3.0` |
| 080 | A6 - Reset discards decay tracks (REQ-3) | Auto | `update(100)`, `reset()`, `update(10.5, 7.0)` | `7.0` | `current_peak()` returns `7.0` |
| 090 | A7 - History unchanged after rejected update (REQ-10) | Auto | `update(5.0, 1.0)`, `update(4.9, 9.0)` | `1.0` | `current_peak()` returns `1.0` |
| 100 | A8 - Monotonic contract restarts at reset (REQ-2) | Auto | `update(100.0, 1.0)`, `reset()`, `update(10.0, 7.0)` | `7.0` | No exception, returns `7.0` |
| 110 | H1 - Departed high is dropped instantly (REQ-8) | Auto | `decay=None, (0,100), (9,40), (10.5,0)` | `40.0` | `current_peak()` returns `40.0` |
| 120 | H2 - Exclusion is not a zero (REQ-8) | Auto | `decay=None, (0,-5), (11,-20)` | `-20.0` | `current_peak()` returns `-20.0` |
| 130 | D1 - Decay track computes correctly (REQ-9) | Auto | `decay=15.0, (0,100), (9,40), (12,0)` | `70.0` | `current_peak()` returns `70.0` |
| 140 | D2 - Window max is the only floor (REQ-6) | Auto | D1 + `update(15.0, 0.0)` | `40.0` | `current_peak()` returns `40.0` |
| 150 | D3 - New high beats the track (REQ-9) | Auto | D1 + `update(12.5, 80.0)` | `80.0` | `current_peak()` returns `80.0` |
| 160 | D4 - Every departed high keeps its own track (REQ-9) | Auto | `decay=15.0, (0,100), (5,90), (16,0)` | `75.0` | `current_peak()` returns `75.0` |
| 170 | D5 - Reads are pure (REQ-4) | Auto | D1 + three calls to `current_peak()` | `70.0` | All return `70.0`, state unchanged |
| 180 | T1 - All-time never ages out (REQ-7) | Auto | `window=None, (0,100), (1000000,5)` | `100.0` | `current_peak()` returns `100.0` |
| 190 | T2 - All-time ignores decay (REQ-7) | Auto | `window=None, decay=15.0, (0,100), (1000000,5)` | `100.0` | `current_peak()` returns `100.0` |
| 200 | V1 - Window must be positive (REQ-11) | Auto | `Telltale(0)` or `Telltale(-5.0)` | Exception | Raises `ValueError` |
| 210 | V2 - Decay rate must be positive (REQ-12) | Auto | `decay_rate=0` or `decay_rate=-3.0` | Exception | Raises `ValueError` |
| 220 | V3 - Decreasing timestamp raises (REQ-10) | Auto | `update(5.0, 1.0)`, `update(4.9, 1.0)` | Exception | Raises `ValueError` |
| 230 | V4 - Equal timestamp is accepted (REQ-13) | Auto | `update(5.0, 1.0)`, `update(5.0, 3.0)` | `3.0` | Accepts, returns `3.0` |
| 240 | Constructor accepts valid configs (REQ-1) | Auto | `Telltale(10.0, 1.0)` | Instance | Creates a `Telltale` instance |

### 10.2 Test Commands

```bash

# Run unit tier for telltale component tests explicitly asserting strict value returns
poetry run pytest tests/unit/test_telltale.py -v --cov=boostgauge.telltale --cov-fail-under=100
```

### 10.3 Manual Tests (Only If Unavoidable)

N/A - All scenarios automated.

## 11. Risks & Mitigations

| Risk | Impact | Likelihood | Mitigation |
|------|--------|------------|------------|
| Out of order timestamps corrupting decay and window arithmetic | High | Low | Validated natively inside the `update` function ensuring strict monotonic progression via `ValueError` |

## 12. Definition of Done

### Code
- [ ] Implementation complete and linted
- [ ] Code comments reference this LLD

### Tests
- [ ] All test scenarios pass
- [ ] Test coverage meets threshold

### Documentation
- [ ] LLD updated with any deviations
- [ ] Implementation Report (0103) completed
- [ ] Test Report (0113) completed if applicable

### Review
- [ ] Code review completed
- [ ] User approval before closing issue

### 12.1 Traceability (Mechanical - Auto-Checked)

Mechanical validation automatically checks:
- Every file mentioned in this section must appear in Section 2.1
- Every risk mitigation in Section 11 should have a corresponding function in Section 2.4 (warning if not)

---

## Appendix: Review Log

### Initial Review #{1} (PENDING)

**Reviewer:** Self
**Verdict:** PENDING

#### Comments

| ID | Comment | Implemented? |
|----|---------|--------------|
| N/A | Document drafted strictly to operator specs | YES |

### Review Summary

| Review | Date | Verdict | Key Issue |
|--------|------|---------|-----------|
| 1 | 2026-09-01 | APPROVED | `claude-opus-4-6` |
| Self #1 | (auto) | PENDING | Draft complete |

**Final Status:** APPROVED