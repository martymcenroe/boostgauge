# #41 - Feature: Telltale peak-hold needle logic (pure, no GUI)

## 1. Context & Goal
* **Issue:** #41
* **Objective:** Add the peak-hold "telltale" logic as a pure class (`Telltale`) that tracks the maximum value reached over a sliding time window with optional linear decay, fully decoupled from GUI and wall-clock time.
* **Status:** Approved (claude-opus-4-6, 2026-09-01)
* **Related Issues:** #1, #2, #7, #35, #36, #125, #270, #315, #316, #424

### Open Questions
None.

## 2. Proposed Changes

### 2.1 Files Changed

| File | Change Type | Description |
|------|-------------|-------------|
| `src/boostgauge/telltale.py` | Add | Pure logic class `Telltale` implementing peak-hold semantics. |
| `tests/unit/test_telltale.py` | Add | Unit tests asserting the acceptance criteria for `Telltale`. |

### 2.1.1 Path Validation (Mechanical - Auto-Checked)

Mechanical validation automatically checks:
- All "Modify" files must exist in repository
- All "Delete" files must exist in repository
- All "Add" files must have existing parent directories
- No placeholder prefixes (`src/`, `lib/`, `app/`) unless directory exists

### 2.2 Dependencies

```toml

# pyproject.toml additions (if any)

# None. This module is pure Python standard library.
```

### 2.3 Data Structures

```python
from typing import Optional, NamedTuple
from collections import deque

class Sample(NamedTuple):
    timestamp: float
    value: float
```

### 2.4 Function Signatures

```python
class Telltale:
    def __init__(self, window: Optional[float], decay_rate: Optional[float] = None):
        """Construct a telltale with a window duration and optional decay rate."""
        ...

    def update(self, timestamp: float, value: float) -> None:
        """Feed one sample to the telltale history."""
        ...

    def current_peak(self) -> Optional[float]:
        """Return the maximum contribution among held samples."""
        ...

    def reset(self) -> None:
        """Discard history, keeping configuration intact."""
        ...
```

### 2.5 Logic Flow (Pseudocode)

```
1. __init__:
   - IF window is not None and window <= 0: raise ValueError
   - IF decay_rate is not None and decay_rate <= 0: raise ValueError
   - Initialize empty history (deque for window max) and empty tracking lists

2. update(timestamp, value):
   - IF timestamp < max_fed_timestamp (since reset): raise ValueError
   - Set max_fed_timestamp = timestamp
   - Add sample to history
   - Prune aged-out samples from active window
   - Move aged-out samples to departed tracks

3. current_peak():
   - IF history is empty: return None
   - active_max = maximum value of samples in window
   - IF decay_rate is unset:
     - Return active_max (departed tracks excluded entirely)
   - ELSE:
     - For each departed track, calculate: value - decay_rate * (max_fed_timestamp - departure_time)
     - Return maximum of (active_max, max of all decayed tracks)

4. reset():
   - Clear history, departed tracks, and max_fed_timestamp
   - Leave window and decay_rate unchanged
```

### 2.6 Technical Approach

* **Module:** `src/boostgauge/telltale.py`
* **Pattern:** Sliding Window Maximum / Monotonic Deque
* **Key Decisions:** The design strictly decouples time from the wall clock. The `reference time` advances only via the explicitly provided timestamp in `update()`. The amortized O(1) performance target for `update()` and `current_peak()` can be satisfied using a monotonic deque for the active window and tracking the single dominant decay line (since all decay tracks descend at the same rate, their relative vertical ordering never changes). To maintain absolute purity, `current_peak()` performs zero state mutation.

### 2.7 Architecture Decisions

| Decision | Options Considered | Choice | Rationale |
|----------|-------------------|--------|-----------|
| Dependency on GUI | `tkinter` coupled, pure logic | Pure logic | Per ADR and issue scope, rendering belongs to #1, logic to #41. |
| Time progression | System clock (`time.time()`), provided timestamps | Provided timestamps | Determinism. Wall-clock reads make test criteria flaky and untestable by strict values. |
| Missing decay track | Drop aged out, floor to zero | Drop aged out | Operator ruling #125: the window maximum is the only floor, even if negative. Exclusion stands. |

**Architectural Constraints:**
- No visual or GUI dependencies (`tkinter`, `PIL`).
- No wall-clock reads. Fully deterministic from the fed samples.

## 3. Requirements

1. The telltale shall be constructed with a window that is either a number of seconds greater than zero or None meaning all-time, and optionally a decay_rate in units per second greater than zero.
2. WHEN `update(timestamp, value)` is called with a timestamp no lower than every timestamp fed since the most recent `reset()` (or since construction, if never reset), the telltale shall add the sample to its history and set the reference time to the greatest such timestamp.
3. WHEN `reset()` is called, the telltale shall discard its entire history — every held sample, every decay track, and the record of fed timestamps, and nothing else — and shall leave its configuration (`window`, `decay_rate`) unchanged, so that the instance is observationally identical to a freshly constructed one with the same configuration.
4. WHEN `current_peak()` is called, the telltale shall modify no state.
5. WHILE no sample is held — before the first `update()`, and again after any `reset()` until the next `update()` — `current_peak()` shall return None.
6. WHILE at least one sample is held, `current_peak()` shall return the maximum contribution among held samples, per the Peak semantics section.
7. WHERE `window` is None, no sample shall ever age out, so the peak is the maximum value fed since the most recent reset and decay never engages.
8. WHERE `decay_rate` is unset, an aged-out sample shall be dropped from the peak read entirely — excluded from the maximum, not counted as a zero.
9. WHERE `decay_rate` is set, an aged-out sample shall contribute its value minus `decay_rate` times the elapsed time since its departure.
10. IF `update()` is called with a timestamp lower than a timestamp fed since the most recent `reset()` (or since construction, if never reset) THEN the telltale shall raise ValueError and leave its history unchanged.
11. IF constructed with a window that is neither None nor a number greater than zero THEN the constructor shall raise ValueError.
12. IF constructed with a decay_rate that is given (not None) and is not a number greater than zero THEN the constructor shall raise ValueError.
13. Equal timestamps are accepted: WHEN two samples share a timestamp, the telltale shall hold both, and the reference time does not move.

## 4. Alternatives Considered

| Option | Pros | Cons | Decision |
|--------|------|------|----------|
| Mix GUI and Logic | Single file/component | Violates separation of concerns, impossible to test purely | **Rejected** |
| Pure Logic Class | 100% testable, O(1) ops | Requires wiring layer to render | **Selected** |

**Rationale:** Option C from `docs/design/0001-test-strategy.md` requires strict visual decoupling. Telltale logic is pure state projection.

## 5. Data & Fixtures

### 5.1 Data Sources

| Attribute | Value |
|-----------|-------|
| Source | Function arguments (timestamp, value) |
| Format | `float` |
| Size | N/A |
| Refresh | On method call |
| Copyright/License | N/A |

### 5.2 Data Pipeline

```
Caller ──update(ts, val)──► Telltale ──current_peak()──► Caller
```

### 5.3 Test Fixtures

| Fixture | Source | Notes |
|---------|--------|-------|
| Test scenarios | Issue text acceptance criteria | Fully deterministic |

### 5.4 Deployment Pipeline

No external data source. Shipped within the `boostgauge` python package.

## 6. Diagram
N/A

## 7. Security & Safety Considerations

### 7.1 Security

| Concern | Mitigation | Status |
|---------|------------|--------|
| Input injection | Type boundaries and pure numeric validation | Addressed |

### 7.2 Safety

| Concern | Mitigation | Status |
|---------|------------|--------|
| Memory exhaustion | Prune aged-out items from internal structures | Addressed |
| Desync via backward time | `ValueError` on decreasing timestamps, history left intact | Addressed |

**Fail Mode:** Fail Closed - Decreasing timestamps raise immediately rather than corrupting window logic.

**Recovery Strategy:** Catch `ValueError` or call `reset()` to restart the monotonic contract cleanly.

## 8. Performance & Cost Considerations

### 8.1 Performance

| Metric | Budget | Approach |
|--------|--------|----------|
| Latency | O(1) amortized | Monotonic deque for sliding window, dominant track caching |
| Memory | O(W) where W is ticks in window | Pruning departed samples that are eclipsed |
| API Calls | 0 | Pure local computation |

**Bottlenecks:** Unbounded `window=None` retains the maximum, avoiding unbounded memory growth for all-time tracking.

### 8.2 Cost Analysis

| Resource | Unit Cost | Estimated Usage | Monthly Cost |
|----------|-----------|-----------------|--------------|
| N/A | $0 | Local execution | $0 |

**Cost Controls:**
- [x] Fallback to cheaper alternatives when appropriate (N/A)

**Worst-Case Scenario:** High frequency updates. Resolved by O(1) update complexity.

## 9. Legal & Compliance

| Concern | Applies? | Mitigation |
|---------|----------|------------|
| PII/Personal Data | No | N/A |
| Third-Party Licenses | No | Pure Python standard library |
| Terms of Service | No | N/A |
| Data Retention | Yes | Memory dropped on `reset()` |
| Export Controls | No | N/A |

**Data Classification:** Internal

**Compliance Checklist:**
- [x] No PII stored without consent
- [x] All third-party licenses compatible with project license
- [x] External API usage compliant with provider ToS
- [x] Data retention policy documented

## 10. Verification & Testing

**Testing Philosophy:** Strive for 100% automated test coverage. This module is purely mathematical; 100% test coverage is mandated.

### 10.0 Test Plan (TDD - Complete Before Implementation)

**Coverage Target:** 100% (pure logic)

### 10.1 Test Scenarios

| ID | Scenario | Type | Input | Expected Output | Pass Criteria |
|----|----------|------|-------|-----------------|---------------|
| 010 | V1 - Window None allows creation (REQ-1) | Auto | `Telltale(None)` | Object instance | No exception |
| 020 | A1 - Update sets reference time and adds sample (REQ-2) | Auto | `Telltale(10.0)`, `update(3.0, 42.5)` | 42.5 | `current_peak()` == 42.5 |
| 030 | A6 - Reset discards decay tracks (REQ-3) | Auto | `Telltale(10.0, 15.0)`, `update(0.0, 100.0)`, `reset()`, `update(10.5, 7.0)` | 7.0 | `current_peak()` == 7.0 (not 92.5) |
| 040 | D5 - current_peak modifies no state (REQ-4) | Auto | D1 samples, 3x `current_peak()` calls | 70.0, 70.0, 70.0 | Pure reads do not mutate tracks |
| 050 | N1 - Fresh construction reads None (REQ-5) | Auto | `Telltale(10.0)` | None | `current_peak()` is None |
| 060 | A4 - Closed boundary in-window semantics (REQ-6) | Auto | `Telltale(10.0)`, `update(0.0, 100.0)`, `update(10.0, 0.0)` | 100.0 | Exact age 10.0 is in-window |
| 070 | T1 - All-time window never ages out (REQ-7) | Auto | `Telltale(None)`, `update(0.0, 100.0)`, `update(1000000.0, 5.0)` | 100.0 | Max fed is kept indefinitely |
| 080 | H1 - Unset decay drops aged-out sample entirely (REQ-8) | Auto | `Telltale(10.0)`, `update(0.0, 100.0)`, `update(9.0, 40.0)`, `update(10.5, 0.0)` | 40.0 | Peak drops instantly to window max |
| 090 | D1 - Set decay reduces aged-out contribution (REQ-9) | Auto | `Telltale(10.0, 15.0)`, `update(0.0, 100.0)`, `update(9.0, 40.0)`, `update(12.0, 0.0)` | 70.0 | 100.0 - 15.0 * 2.0 = 70.0 |
| 100 | V3 - Decreasing timestamp raises ValueError (REQ-10) | Auto | `Telltale(10.0)`, `update(5.0, 1.0)`, `update(4.9, 1.0)` | ValueError | Raises ValueError |
| 110 | V1 - Invalid window raises ValueError (REQ-11) | Auto | `Telltale(0)` | ValueError | Raises ValueError |
| 120 | V2 - Invalid decay_rate raises ValueError (REQ-12) | Auto | `Telltale(10.0, 0)` | ValueError | Raises ValueError |
| 130 | A5 - Equal timestamps are accepted (REQ-13) | Auto | `Telltale(10.0)`, `update(5.0, 1.0)`, `update(5.0, 3.0)` | 3.0 | Both accepted without exception |
| 140 | N2 - Reset discards history and reads None (REQ-5) | Auto | `Telltale(10.0)`, `update(0.0, 100.0)`, `reset()` | None | `current_peak()` is None |
| 150 | A2 - Rising series (REQ-6) | Auto | `Telltale(10.0)`, `(0.0, 10.0)`, `(1.0, 20.0)`, `(2.0, 30.0)` | 30.0 | New high registers immediately |
| 160 | A3 - In-window values never decay (REQ-9) | Auto | `Telltale(10.0, 15.0)`, `update(0.0, 100.0)`, `update(5.0, 0.0)` | 100.0 | 100.0 has not departed, no decay |
| 170 | A7 - History unchanged after rejected update (REQ-10) | Auto | `Telltale(10.0)`, `update(5.0, 1.0)`, `update(4.9, 9.0)` | 1.0 | Prior state survives rejection |
| 180 | A8 - Monotonic contract restarts at reset (REQ-10) | Auto | `Telltale(10.0)`, `update(100.0, 1.0)`, `reset()`, `update(10.0, 7.0)` | 7.0 | Earlier timestamp accepted post-reset |
| 190 | H2 - Exclusion is not a zero (REQ-8) | Auto | `Telltale(10.0)`, `update(0.0, -5.0)`, `update(11.0, -20.0)` | -20.0 | Peak may be negative, zero is not a floor |
| 200 | D2 - Decay floor is window max (REQ-9) | Auto | D1 samples, `update(15.0, 0.0)` | 40.0 | Decay bounded by 40.0 (window max) |
| 210 | D3 - New high beats the track (REQ-9) | Auto | D1 samples, `update(12.5, 80.0)` | 80.0 | In-window beats decayed track |
| 220 | D4 - Every departed high keeps its own track (REQ-9) | Auto | `Telltale(10.0, 15.0)`, `(0.0, 100.0)`, `(5.0, 90.0)`, `(16.0, 0.0)` | 75.0 | Track of 90.0 beats track of 100.0 |
| 230 | T2 - All-time ignores decay (REQ-7) | Auto | `Telltale(None, 15.0)`, `update(0.0, 100.0)`, `update(1000000.0, 5.0)` | 100.0 | No decay engages |
| 240 | V4 - Equal timestamps raise nothing (REQ-13) | Auto | `Telltale(10.0)`, `update(5.0, 1.0)`, `update(5.0, 3.0)` | 3.0 | Accepted seamlessly |

### 10.2 Test Commands

```bash

# Run all automated tests
poetry run pytest tests/unit/test_telltale.py -v
```

### 10.3 Manual Tests (Only If Unavoidable)

N/A - All scenarios automated.

## 11. Risks & Mitigations

| Risk | Impact | Likelihood | Mitigation |
|------|--------|------------|------------|
| Logic execution is too slow, failing O(1) target | Med | Low | Implement amortized queue strategies (e.g. `deque`) within `__init__`, `update` and `current_peak`. |
| Flaky tests due to `time.time()` | High | Low | Pure API guarantees wall-clock isolation. All tests use literal floats in `update` calls. |

## 12. Definition of Done

### Code
- [ ] Implementation complete and linted
- [ ] Code comments reference this LLD

### Tests
- [ ] All test scenarios pass
- [ ] Test coverage meets threshold

### Documentation
- [ ] LLD updated with any deviations
- [ ] Implementation Report (0103) completed
- [ ] Test Report (0113) completed if applicable

### Review
- [ ] Code review completed
- [ ] User approval before closing issue

### 12.1 Traceability (Mechanical - Auto-Checked)

- `src/boostgauge/telltale.py` is in Section 2.1
- `tests/unit/test_telltale.py` is in Section 2.1
- Mitigation functions (`__init__`, `update`, `current_peak`) are in Section 2.4

---

## Appendix: Review Log

### Review Summary

| Review | Date | Verdict | Key Issue |
|--------|------|---------|-----------|
| 1 | 2026-09-01 | APPROVED | `claude-opus-4-6` |
| None | (auto) | PENDING | Initial Draft |

**Final Status:** APPROVED