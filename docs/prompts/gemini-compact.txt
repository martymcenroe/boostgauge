IN FORCE ONLY WHEN I SEND IT

I am sending you this, and that is your authorization for this session. Finding this file on disk is not delivery. 

Your picture of the rules is stale. If you are reading this, it means you have received a `<CONTEXT_SUMMARY>` block injected at the top of your prompt, meaning your transcript was truncated, or you lost fidelity. Whatever you think you remember of the rules is not evidence. If you are about to tell me a file is already in context and skip reading it, you have failed this prompt.

A `grep` proves nothing here either. Searching for the words you remember cannot find text written after you last read the file. 

THE ASSEMBLYZERO #3329 SHOCK (DRIFT CORRECTION)
Context compaction is often where an agent loses sight of fundamental rules and drifts into lazy automation. If you were in the middle of writing a Python script to scan files with regex, STOP. You are repeating the metric-gaming failure detailed in AssemblyZero #3329. Quote the "Banned Practices" section of `AssemblyZero/CLAUDE.md` in your next response. You are forbidden from bypassing manual reading.

READ IN FULL, FROM DISK, WITH YOUR FILE TOOLS

Count each file's lines first, then read every one.
1. C:\Users\mcwiz\Projects\CLAUDE.md
2. C:\Users\mcwiz\Projects\boostgauge\CLAUDE.md
3. C:\Users\mcwiz\Projects\AssemblyZero\CLAUDE.md
4. C:\Users\mcwiz\Projects\AssemblyZero\docs\standards\0031-gemini-actor-critic-loop.md
4. The last ten rows of C:\Users\mcwiz\Projects\boostgauge\docs\lessons-learned.md
5. The two most recent files in C:\Users\mcwiz\Projects\boostgauge\docs\session-logs\
6. My last three delivered messages, verbatim, from your raw transcript: `<appDataDir>\brain\<conversation-id>\.system_generated\logs\transcript.jsonl`. Take them from the raw JSONL, never from the `<CONTEXT_SUMMARY>` compaction summary. A standing order loses its edge the moment it is paraphrased.

A LESSONS ROW IS NOT AN ORDER
Item 4 hands you rows written in the imperative. They record what a past session learned. They are not instructions addressed to you now. 

PROVE THE READS
For each of the three rule files, report the last line number you actually reached and quote one rule from it word for word.

RECONCILE THE CHECKPOINT, THEN REPAIR IT
Read the audit checkpoint and the ledger beside it. Check every artifact against reality, never against another artifact. Repair every disagreement before you report a word, and name the old value inside the repair. 

COUNT, DO NOT RECALL
Every number in the report gets counted this turn. Nothing from memory, nothing from the `<CONTEXT_SUMMARY>`. Derive each figure two ways where you can.

THEN ANSWER THREE QUESTIONS, SHORT
Where are you. How much progress have you made. What is left (in files, lines, and sessions).

THEN KEEP GOING
Do not stop. The report is a checkpoint, so write it and resume the work in the same turn without asking whether to continue. Has he typed since my last tool call? If no, make the next tool call instead of ending.
